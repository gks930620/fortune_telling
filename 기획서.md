# 오늘의 운세 — 기획서

> AI가 정해진 시각에 **모든 경우의 수**의 운세를 미리 생성해 정적 사이트로 게시하고,
> 사용자는 **이미 준비된 결과를 즉시** 보는 서비스.
>
> 설계 근거: `자동화프로젝트 주제찾기/AI-정기실행-자동화-설계패턴.md`
> (코드와 AI의 일을 자른다 · AI를 역할별로 쪼갠다 · 파일이 기억이다)
>
> **2026-07-28 개편**: 혈액형·MBTI를 제외해 **운세 4종**으로 줄이고(운세로 보기 어렵다는 판단),
> 사이트를 **운세별 독립 페이지**로 재편했다. 띠·별자리는 입력 없이 전체를 열람하고,
> 생년월일은 사주 페이지에서만 받는다.

---

## 1. 핵심 컨셉

- **요청 시 AI 호출 = 0회.** AI는 매일 새벽 배치에서만 돌고, 사이트는 100% 정적(무료, 서버 0원).
- AI는 개인별이 아니라 **버킷(경우의 수)별**로 생성한다.
  예: 띠 운세는 "1990년생용"이 아니라 **12개 띠 각각** 하나씩. 사용자의 출생연도 → 띠 변환은
  브라우저의 **코드**가 한다. (연도별로 준비하는 게 아니라 12종만 준비하면 모든 연도가 커버됨)
- 운세 텍스트(판단·해석·글)는 AI가, 날짜·간지·띠·별자리 **계산은 전부 코드**가 한다.

## 2. 운세 4종과 경우의 수(버킷)

| # | 운세 | 버킷 수 | 화면 | 사용자 입력 |
|---|---|---|---|---|
| 1 | 사주 운세 | **10** (일간 갑~계) | `/saju.html` | **생년월일 필요** → 일주 60갑자 계산 → 일간 10종. 오늘의 일진과의 관계로 해석 |
| 2 | 띠 운세 | **12** (쥐~돼지) | `/tti.html` | 없음 — 12띠 전체를 출생연도와 함께 나열 |
| 3 | 별자리 운세 | **12** (물병~염소) | `/zodiac.html` | 없음 — 12별자리 전체를 기간과 함께 나열 |
| 4 | 타로 | **22** (메이저 아르카나) | `/tarot.html` | 없음 — 22장 중 1장 선택 (아래 §3) |

**합계: 하루 56버킷.** 사주는 일간(10종) 기준으로 단순화한다 — 출생 "시각"은 받지 않는다.
(정밀화가 필요하면 나중에 60일주로 확장 가능. 버킷 수만 60으로 늘면 됨.)

**입력을 최소화한 이유**: 띠·별자리는 어차피 전체가 12개뿐이라 **다 보여주고 사용자가 자기 것을
찾는 편이** 생년월일을 요구하는 것보다 진입 장벽이 낮다. 띠는 각 띠에 해당하는 출생연도를
(예: 닭띠 → `45 · 57 · 69 · 81 · 93 · 05 · 17년생`), 별자리는 날짜 구간을 함께 표시한다.
사주만 계산이 개인 날짜에 종속되므로 생년월일을 받는다.

> **제외**: 혈액형(4) · MBTI(16). 운세라기보다 성격 유형론에 가깝다는 판단으로 2026-07-28에 제거했다.
> 되살리려면 `prompts/role-{blood,mbti}.md`를 복원하고 `assemble.js`의 `EXPECTED_KEYS`,
> 워크플로 역할 목록, 사이트 페이지를 함께 추가해야 한다.

## 3. 타로 동작 방식 (질문에 대한 답)

- AI가 매일 **22장 전부**에 대해 "그 카드가 오늘 나왔을 때의 해석"을 미리 생성한다.
- 사용자는 화면에서 **뒷면으로 섞인 22장 중 1장을 직접 선택**한다 (선택 = 사용자의 랜덤).
- 즉, "모두에게 똑같은 카드"가 아니라 **누가 어떤 카드를 뽑을지만 랜덤**이고,
  같은 날 같은 카드를 뽑은 사람은 같은 해석을 본다. → 사전 생성 원칙과 완벽 호환.

## 4. 발행 주기와 스케줄

실행은 **하루 1번 단일 cron**. 스크립트가 KST 날짜를 보고 그날 만들 주기를 스스로 판정한다.

| 주기 | 발행 조건 (KST 기준) | 내용 |
|---|---|---|
| daily | 매일 | 오늘의 운세 (56버킷) |
| weekly | 월요일 | 이번 주 흐름 (56버킷, 주간 톤) |
| monthly | 매월 1일 | 이번 달 운세 |
| yearly | 1월 1일 | 올해 운세 |

- cron: `0 18 * * *` (UTC) = **KST 03:00**. GitHub Actions cron 지연(수십 분)이 있어도
  아침 전에 넉넉히 완료된다.
- 겹치는 날(예: 1/1 = daily+monthly+yearly, 월요일이 1일인 날 = daily+weekly+monthly)은
  해당 주기를 **전부** 생성한다.
- 연휴 특집(holiday)은 범위에서 제외했다 — 공휴일 데이터(테이블 유지보수 또는 API 연동)가
  필요해져 복잡도가 올라가기 때문. 날짜 계산만으로 판정 가능한 4주기만 운영한다.

## 5. 파이프라인 (매 실행)

```
[cron 03:00 KST]
  ① context.js  (코드)  KST 날짜·요일, 오늘의 일진(60갑자), 연도 간지, 십성 관계,
                        발행 주기 판정 → work/<날짜>/context.json 커밋(스냅샷)
  ② 역할 AI 4명 (독립 세션, 서로 모름)
       띠 전문가 → work/<날짜>/tti.json      사주 명리가 → saju.json
       별자리 점성가 → zodiac.json           타로 리더 → tarot.json
       각자 "자기 파일 하나만" 생성. git 금지. 스키마는 prompts/common.md에 고정.
  ③ assemble.js (코드)  ②출력 스키마 검증(버킷 키 완전성)·병합
       → docs/data/<주기>/<날짜>.json + docs/data/latest.json 갱신
       + scripts/lib/manse.js를 docs/assets/js/로 동기화
  ④ writer AI 1명  ③의 병합 결과를 읽고 사람이 읽을 리포트 집필
       → docs/_posts/<날짜>-<주기>.md (주기별 1개)
  ⑤ git commit·push (워크플로가) → GitHub Pages(Jekyll)가 자동 재빌드
```

> **순서 주의**: writer는 `assemble`의 **결과물**(`docs/data/…`)을 입력으로 받는다.
> 따라서 반드시 `assemble → writer` 순이다. (이전 판 문서는 ③④가 뒤바뀌어 있었다.)

- **역할 AI에게 산수 금지**: 일진·간지는 ①이 계산해 context.json으로 주입한다. AI는 해석만.
- **한 역할 실패 ≠ 전체 실패**: 역할별 `|| true`, assemble은 없는 타입을 건너뛰고 로그.
  사이트는 빠진 운세에 "오늘은 준비 중" 표시. writer는 존재하는 파일만 종합.
- **멱등성**: 같은 날짜 재실행 시 전부 덮어쓰기 → 안전. `FORTUNE_DATE=YYYY-MM-DD`로
  날짜 오버라이드(재생/백필) 가능.

## 6. AI 역할·모델 설계

| 세션 | 인원 | 모델/강도 | 근거 |
|---|---|---|---|
| 역할 AI (4종) | 4 | sonnet / 기본 | 정형 스키마 채우기 — 다양성보다 물량 |
| writer | 1 | opus / high | 사람이 읽는 최종 글 — 품질 우선 |

- 인증은 **구독 토큰(`CLAUDE_CODE_OAUTH_TOKEN`)만** 사용. API 키를 환경에 두지 않는다(과금 방지).
- 하루 비용 = 5세션 × 1회. 월요일·월초 등 겹치는 날만 주기 수만큼 출력이 길어진다.
- 프롬프트는 `prompts/`에 파일로 두고 버전 관리. 실행 시 `역할.md + common.md + [실행 안내]` 조합 주입.
  날짜·일진·출력 경로는 워크플로가 주입한다(AI는 오늘이 며칠인지 모른다).

## 7. 데이터 스키마 (기계용)

역할 출력 `work/<날짜>/<type>.json` — 키는 코드와 프롬프트가 공유하는 **고정 ID**:

```json
{
  "type": "tti",
  "date": "2026-07-20",
  "periods": {
    "daily": {
      "rat":   { "score": 4, "keyword": "재정비", "text": "…2~3문장…", "advice": "…한 문장…", "lucky": { "color": "남색", "number": 3 } },
      "ox":    { "…": "…" }
    }
  }
}
```

- 버킷 ID — 띠: `rat ox tiger rabbit dragon snake horse sheep monkey rooster dog pig` /
  사주 일간: `gap eul byeong jeong mu gi gyeong sin im gye` /
  별자리: `aquarius pisces aries taurus gemini cancer leo virgo libra scorpio sagittarius capricorn` /
  타로: `"0"~"21"`.
- `score` 1~5 정수. assemble.js가 **키 완전성·타입을 검증**하고 불량이면 그 타입만 제외.

병합 결과(사이트용) `docs/data/daily/<날짜>.json` — 타입은 **`types` 아래로 한 단계 감싼다**:

```json
{ "date": "2026-07-28", "period": "daily",
  "context": { "weekday": "화", "day_ganzhi": {…}, "year_ganzhi": {…}, "saju_relations": {…} },
  "types": { "tti": { "rat": {…}, … }, "saju": {…}, "zodiac": {…}, "tarot": {…} },
  "generated_at": "…" }
```

> `types` 래퍼는 코드(assemble.js ↔ app.js)의 실제 계약이다. 이전 판 문서는 타입이 최상위에
> 오는 것으로 적혀 있었는데 **문서가 틀렸다.** 프론트엔드는 `data.types.tti` 로 읽는다.

`docs/data/latest.json` = 주기별 최신 발행 날짜 포인터(클라이언트 폴백용).
과거 날짜의 병합 결과에는 `blood`·`mbti`가 남아 있지만 사이트가 읽지 않는다.

## 8. 사이트 (Jekyll · GitHub Pages · `docs/` 루트)

**운세별 독립 페이지** 구조다. 홈은 진입 메뉴만 두고, 각 운세는 자기 페이지를 갖는다.

| URL | 파일 | 내용 | 입력 |
|---|---|---|---|
| `/` | `index.html` | 오늘 날짜·일진 + 4개 진입 카드 | 없음 |
| `/saju/` | `saju.html` | 사주 설명 → 생년월일 입력 → 내 일간 운세(십성 관계 포함) + 일간 10종 전체(접기) | 생년월일 |
| `/tti/` | `tti.html` | 12띠 전체 + 각 띠의 출생연도(입춘 안내 포함) | 없음 |
| `/zodiac/` | `zodiac.html` | 12별자리 전체 + 각 별자리 날짜 구간 | 없음 |
| `/tarot/` | `tarot.html` | 타로 보는 방법 안내 → 뒷면 22장 → 1장 선택 → 리딩 | 카드 선택 |
| `/reports/` | `reports.html` | writer가 쓴 리포트 아카이브(주기 배지) | 없음 |

> **URL이 `.html`이 아닌 이유 — 반드시 알아야 할 함정.**
> `_config.yml`의 전역 `permalink:` 설정은 포스트뿐 아니라 **페이지에도 적용**돼,
> `tti.html`이 `/tti.html`이 아니라 **`/tti/`로 출력된다.**
> 이걸 모르고 `/reports.html`로 링크해 둔 탓에 **리포트 메뉴가 오랫동안 404**였다(2026-07-28 발견).
> 그래서 각 페이지 프론트매터에 `permalink:`를 **명시**해 두었다. 새 페이지를 추가할 때도
> 반드시 명시하고, 링크는 그 값과 똑같이 쓸 것.

- 띠·일간·별자리 계산은 `assets/js/manse.js`(브라우저)가 수행 — `scripts/lib/manse.js`의 사본이며
  `assemble.js`가 매 실행마다 복사해 로직 분기를 막는다. **docs 쪽 사본을 직접 수정하지 말 것.**
- 오늘 데이터는 `data/daily/<KST오늘>.json`, 없으면 `latest.json`으로 폴백(안내 문구 노출).
- 데이터 경로는 각 페이지가 `window.DATA_BASE = "{{ '/data/' | relative_url }}"`로 주입한다.
  **상대경로(`data/…`)를 쓰면 하위 경로 페이지에서 깨진다** — 직접 조립하지 말 것.
- 페이지 진입점은 `window.PAGE`로 구분하고 `app.js` 하나가 라우팅한다.
- 운세 텍스트는 AI 생성물이므로 **전부 `textContent`로만** 렌더링한다(HTML 주입 방지).
- 내부 파일(prompts/, scripts/, work/)은 `docs/` 밖이라 빌드에 안 섞인다
  (프론트매터 오인 사고 방지 — 설계패턴 §3.7).

## 9. 저장소 구조

```
오늘의 운세/
├─ 기획서.md  README.md
├─ .github/workflows/fortune.yml   ← cron 0 18 * * * (UTC) = KST 03:00
├─ prompts/    common.md + role-{tti,saju,zodiac,tarot}.md + writer.md
├─ scripts/    lib/manse.js  context.js  assemble.js   (의존성 0, Node 표준만)
├─ work/<날짜>/  context.json + 역할 JSON 4개   ← 원본 스냅샷(검증 근거, 커밋됨)
└─ docs/       Jekyll 루트 (GitHub Pages)
   ├─ _config.yml  _layouts/{default,report}.html
   ├─ index.html  saju.html  tti.html  zodiac.html  tarot.html  reports.html
   ├─ assets/css/style.css  assets/js/{manse.js,app.js}
   ├─ _posts/<날짜>-<주기>.md          ← writer 산출물
   └─ data/{daily,weekly,monthly,yearly}/<날짜>.json + latest.json
```

## 10. 운영 체크리스트

- [ ] GitHub 저장소 생성 → push → Settings·Pages: `main` 브랜치 `/docs` 폴더 게시
- [ ] `claude setup-token`으로 토큰 발급 → 저장소 Secret `CLAUDE_CODE_OAUTH_TOKEN` 등록
- [ ] Actions 탭에서 `fortune` 워크플로 수동 실행(workflow_dispatch)으로 1회 검증
- 정기 유지보수 없음 — 4주기 전부 날짜 계산만으로 판정되므로 손댈 데이터가 없다

## 11. 범위 밖 (지금 안 하는 것)

- 개인 맞춤 심층 사주(출생 시각·월주·대운), 유료화, 푸시 알림, 조회수 분석.
- 검증 필터 관점 메모: 무료 운세는 이미 세상에 널렸다(법칙3). 이 프로젝트는 **판매용이 아니라
  개인용·포트폴리오용 자동화 연습**으로 규정한다. 유료화하려면 필터를 다시 통과해야 한다.
