# 오늘의 운세 — 현황 점검

> 기준일: **2026-07-28** · 점검 범위: 전체 코드 + 원격(origin/main) 산출물 9일치 + 라이브 사이트
> 비교 기준 문서: [기획서.md](기획서.md)

---

## ✅ 2026-07-28 개편 — 이 점검 직후 반영된 것

아래 본문은 **개편 전** 상태를 기록한 것이다. 다음 항목은 이미 고쳤다.

**운세 6종 → 4종.** 혈액형·MBTI 제거(운세로 보기 어렵다는 판단). 하루 76버킷 → **56버킷**.
`prompts/role-{blood,mbti}.md` 삭제, `assemble.js`의 `EXPECTED_KEYS`·워크플로 역할 목록·사이트에서 제거.

**사이트를 운세별 독립 페이지로 재편.** 기존에는 홈에서 생년월일·혈액형·MBTI를 다 받고
한 화면에 6개 카드를 뿌렸는데, 이제 홈은 진입 메뉴만 둔다.

| 페이지 | 내용 | 입력 |
|---|---|---|
| `index.html` | 날짜·일진 + 4개 진입 카드 | 없음 |
| `saju.html` | 사주 설명 → 생년월일 → 내 일간 운세(십성 포함) + 일간 10종 접기 | 생년월일 |
| `tti.html` | **12띠 전체** + 각 띠 출생연도(`닭띠 → 45·57·69·81·93·05·17년생`) | 없음 |
| `zodiac.html` | **12별자리 전체** + 날짜 구간(`쌍둥이자리 → 5월 21일 ~ 6월 21일`) | 없음 |
| `tarot.html` | 타로 보는 법 안내 → 뒷면 22장 → 1장 선택 → 리딩 | 카드 선택 |

띠·별자리는 12개뿐이라 **전체를 보여주고 자기 것을 찾게 하는 쪽**이 입력을 요구하는 것보다 낫다는 판단.
사주만 계산이 개인 날짜에 종속돼 생년월일을 받는다.

**해소된 항목** — 4-5(타로 3종 결함: `lucky.number`=카드번호, keyword 복창, 날짜 무관),
4-1의 타로 부분(탑=항상 1점), 3-3(문서 스키마 드리프트), 6절의 상대경로 지뢰(`window.DATA_BASE` 주입),
프로필 삭제 버튼 부재, `.tarot-result` 죽은 CSS.

**검증**: jsdom으로 5개 페이지를 실제 데이터에 대해 렌더링하는 기능 테스트 **36건 전부 통과**
(띠 12장·별자리 12장·타로 22장 뽑기·사주 일간 계산 일치·데이터 없음 폴백 포함).

**아직 남은 것**: 주간/월간/신년 운세 화면(3-1), 어제 기억 주입(4-3, 최우선), 합·충 코드화(5절),
과거 운세 열람, 07-20 샘플 재생성, 별자리 경계 결정.

---

## 0. 한 줄 요약

**파이프라인은 죽지 않았다. 9일 연속 정상 가동 중이고, 고칠 것은 "제품"이지 "자동화"가 아니다.**

자동화 자체는 기획대로 돌아간다. 문제는 그 위에 있다 — 매일 새 글이 생성되지만
**사용자 입장에서는 어제와 같은 운세로 읽히고**, 애써 만든 주간운세는 **사이트에서 볼 방법이 없다.**

---

## 1. 먼저 짚을 것 — 로컬 저장소가 8커밋 뒤처져 있다

이게 "동작이 내 생각과 다르다"는 인상의 큰 원인일 수 있다.

| | 커밋 | 최신 산출물 |
|---|---|---|
| 로컬 작업본 | `9eb8037` (07-21) | `work/2026-07-21/context.json` **하나뿐** |
| 원격 `origin/main` | `3ec795c` (07-28) | 9일치 전량 |

로컬만 보면 **"07-21 새벽에 context만 만들고 운세 생성이 전멸했다"** 로 보인다.
실제로는 그 뒤로 봇이 8번 더 성공적으로 커밋했고, 로컬이 그걸 안 받아왔을 뿐이다.

```
git pull --rebase        # 먼저 이것부터. 안 하면 계속 유령을 쫓게 된다
```

참고로 로컬에 남은 `work/2026-07-21/context.json`만 있는 상태는 **실재했던 사고**다.
그날 새벽 실행(`64ac6dc`)은 역할 AI 6종이 전부 실패했다. 그 직후 사용자가 워크플로를 두 번
고쳤고(`551fcca` 진단 강화, `9eb8037` 커밋·푸시 순서 수정), **그 이후 07-21부터 오늘까지는 무사고**다.
즉 이미 고쳐진 과거의 상처이고, 로컬에만 그 흉터가 남아 있다.

---

## 2. 지금 실제로 벌어지고 있는 일

라이브: **https://gks930620.github.io/fortune_telling/** (정상 서비스 중, 자산 경로·데이터 로드 모두 200)

| 항목 | 실측값 |
|---|---|
| 연속 성공 | 07-21 ~ 07-28 **9일** (봇 커밋 8건 + 초기 샘플 1일) |
| 매일 생성 버킷 | **76/76** (6타입 전부, 단 하루도 누락 없음) |
| 누적 생성량 | 684버킷 + 리포트 10편 |
| 주간운세 | 07-27(월) 정상 발행 — **한 세션에서 daily+weekly 152버킷** 생성 성공 |
| 저장소 증가 | 하루 약 100KB (연 36MB — 당분간 신경 쓸 필요 없음) |
| 코드 변경 | 07-21 이후 **없음** — 봇이 데이터만 쌓는 중 |

`git archive origin/main`으로 원격 산출물을 받아 직접 검증한 결과다.
**월요일 2주기 동시 생성이 실전에서 성공했다는 건 중요한 신호다** — 1월 1일 3주기(228버킷)도
구조적으로는 갈 가능성이 높다는 뜻이다. (다만 아직 미검증)

---

## 3. 기획서와 실제 동작의 차이

### 3-1. 주간·월간·신년 운세를 사이트에서 볼 수 없다 ★가장 큰 괴리

기획서는 4주기 전부 76버킷씩 만든다고 했고, **실제로 만들고 있다.**
`docs/data/weekly/2026-07-27.json` 안에 12띠·10일간·22타로… 주간운세가 온전히 들어 있다.

그런데 [app.js:42](docs/assets/js/app.js#L42)는 **`data/daily/` 만 읽는다.**

```js
return fetchJSON('data/daily/' + today + '.json').catch(...)   // weekly/monthly/yearly 진입점 없음
```

결과적으로 주간운세는 **writer가 쓴 산문 리포트 한 편**으로만 존재한다.
"내 띠의 이번 주 운세"를 개인화해서 보는 화면이 아예 없다. 만들어 놓고 안 쓰는 데이터다.

> 매달 1일·매년 1월 1일에도 똑같은 일이 벌어진다. monthly/yearly JSON은 생성되지만 사이트엔 안 나온다.

### 3-2. 파이프라인 순서가 기획서와 반대로 적혀 있다

기획서 §5는 `③ writer → ④ assemble` 이지만, 실제 워크플로는 **`assemble → writer`** 다.
writer가 병합 결과(`docs/data/<주기>/<날짜>.json`)를 입력으로 받으니 **실제 구현이 맞고 기획서가 틀렸다.**
(README의 파이프라인 그림은 올바르다.)

### 3-3. 병합 JSON 스키마가 문서와 다르다

기획서 §7은 최상위에 `tti`, `saju`가 바로 오는 형태로 적혀 있지만, 실제
[assemble.js:84](scripts/assemble.js#L84)는 `types` 아래로 한 단계 감싼다.

```jsonc
// 기획서 §7        →  실제
{ "tti": {...} }       { "types": { "tti": {...} } }
```

코드(assemble ↔ app.js)끼리는 일치하므로 **동작에는 문제없다.** 문서만 갱신하면 된다.

---

## 4. 진짜 고쳐야 할 것 — 제품 품질

여기가 핵심이다. **자동화가 아니라 결과물이 문제다.**

### 4-1. 점수가 "그날"이 아니라 "그 버킷"에 고정돼 있다 ★★

9일치 같은 버킷의 점수 추이를 뽑아봤다:

| 버킷 | 07-20 → 07-28 점수 | 문제 |
|---|---|---|
| `zodiac.leo` (사자자리) | 4 **5 5 5 5 5** 4 5 4 | 9일 중 6일이 5점 — 사자자리는 늘 최고 |
| `tarot.16` (탑) | 2 **1 1 1 1 1 1** 1 2 | 9일 중 7일이 1점 — 탑을 뽑으면 항상 최악 |
| `mbti.INTJ` | 3 **4 4 4 4 4 4 4** 4 | 8일 연속 4점 — 변화가 없다 |
| `tti.rat` (쥐띠) | 3 4 3 3 3 4 4 3 2 | 이 정도는 정상 범위 |

**날짜별 전체 분포는 건강하다** (매일 1점 5~6개, 5점 14~18개로 잘 퍼져 있다).
문제는 *어느 버킷이* 높은 점수를 받는지가 매일 똑같다는 것이다.

원인은 프롬프트에 박혀 있다 — [role-tarot.md](prompts/role-tarot.md)가 직접 지시한다:

```
`score`는 그 카드가 오늘 주는 에너지의 밝기로 매겨라 (태양=5 쪽, 탑=1~2 쪽 등)
```

카드의 고정 이미지로 점수를 매기라고 했으니 매일 같은 점수가 나오는 게 당연하다.
**"오늘의 일진과 그 카드의 관계"로 점수가 흔들려야** 매일 뽑을 이유가 생긴다.

### 4-2. 키워드도 매일 같은 말을 돌려 쓴다 ★★

9일간 같은 버킷의 `keyword`를 나열하면 이렇게 된다:

| 버킷 | 9일간의 키워드 |
|---|---|
| `zodiac.leo` | 예열 · **무대주인공 · 존재감 · 스포트라이트 · 존재감 · 스포트라이트 · 무대중심 · 무대체질 · 존재감** |
| `mbti.INTJ` | 계획 내려놓기 · **전략적 여백 · 전략선회 · 유연한 전략 · 여유 · 유연 전략 · 무계획 휴식 · 전략 전환 · 유연한 전략** |
| `tarot.16` | 재건 · 대청소 · **재건 · 재건 · 재건 · 재건 · 재건** · 충격 · 재건 |
| `blood.A` | 섬세함이 무기 · 재검토 · 차분한 정리 · **꼼꼼한 점검 · 꼼꼼한 점검** · 과민모드 · 꼼꼼모드 · 차분한 정리 · 재정비 |

INTJ는 **9일 내리 "계획을 내려놓아라"** 는 말만 듣는다. 사자자리는 늘 무대 위에 있다.
문장 자체는 매일 새로 쓰이지만(완전 중복 문장은 **0건**), 하는 말은 같다.

### 4-3. 왜 이렇게 되나 — 어제를 모르기 때문 ★★★ (근본 원인)

역할 AI는 매일 **독립 세션**으로 뜨고, 입력은 `context.json`(날짜·일진·십성)뿐이다.
**어제 자기가 뭐라고 썼는지 모른다.** 그러니 매일 처음부터 "사자자리 = 화려함 = 좋음",
"INTJ = 계획충", "탑 = 파괴" 라는 **같은 고정관념을 다시 유도해낸다.**

기획서가 인용한 설계 원칙 세 가지 중 **"파일이 기억이다"가 유일하게 적용되지 않은 부분**이다.
`work/`에 9일치 기억이 이미 쌓여 있는데 아무도 읽지 않는다.

가장 효과가 클 수정: **역할 프롬프트에 "최근 3일간 네가 쓴 그 버킷의 keyword" 를 주입하고
"이 단어들과 겹치지 마라"고 지시하는 것.** 코드 몇 줄이면 되고, 4-1·4-2를 동시에 푼다.

### 4-4. 문체가 좁다 ★

684개 text 전수 조사:

| 표현 | 비율 |
|---|---|
| "오늘은"이 들어감 | **37%** (255개) |
| "기운이"가 들어감 | 16% |
| "~날입니다"로 끝나는 문장 포함 | 13% |
| `advice`가 "~보세요"류로 끝남 | **약 45%** (해보세요 20%, 어보세요 8%, …) |

조언 절반이 같은 어미로 끝난다. 표로 나란히 볼 때(리포트) 특히 티가 난다.
`~세요.`로 끝나는 것까지 넓히면 **608개 중 561개, 92%** 다.

### 4-5. 프롬프트를 그대로 복창하는 자리들 ★★

프로덕션 8일(176장 타로 · 80개 사주)을 대조해 확인한, **구조적으로 고정된** 반복이다.

**타로 `lucky.number`가 카드 번호와 같다 — 176개 중 167개(95%).**
광대(0번)를 뽑으면 행운의 숫자가 **0**, 탑(16번)이면 16이다. 두 번만 뽑아보면 규칙이 들킨다.
"행운의 숫자 0"은 문구로서도 성립하지 않는다.

**타로 `keyword`의 53%가 [role-tarot.md](prompts/role-tarot.md) 괄호 안 단어의 복사다** (176개 중 93개).
프롬프트에 `13 죽음(마무리와 재생)`이라 써두니 keyword가 토씨까지 `"마무리와 재생"`으로 나온다.
괄호 안은 카드 이해용 참고어인데 **출력 필드로 직행**하고 있다.

**타로가 오늘 날짜와 거의 무관하다.** 22장 중 그날 일진·오행·요일에 결부된 카드는:

| 날짜 | 결부된 카드 |
|---|---|
| 07-26 | 22장 중 **4장** |
| 07-27 | 22장 중 7장 |
| 07-28 | 22장 중 8장 |

나머지는 **아무 날에 붙여도 그대로 성립하는 문장**이다. 매일 22장을 새로 생성하는 비용의
상당 부분이 여기서 낭비된다. (반면 띠·사주는 12/12, 10/10이 그날 일진에서 도출된다.)

**사주 10개의 첫 문장이 매일 10/10 같은 골격이다.**
[role-saju.md](prompts/role-saju.md)의 예시 `(예: 갑목(甲木)인 당신은…)`이 오프닝을 고정시켰다.
게다가 리포트 사주 표에 "오늘의 십성" 열이 이미 있어서, 본문 첫 문장이 **표를 되읽는 중복**이 된다.

> 공통 패턴: **프롬프트의 "예시"가 출력의 "정답"이 되고 있다.** 예시를 줄이거나
> "이 문장 자체는 쓰지 말고 방향만 참고하라"를 명시하면 상당 부분 풀린다.

### 4-6. ⚠️ 저장소의 07-20 샘플이 실제 품질을 왜곡한다

`work/2026-07-20/`과 `docs/_posts/2026-07-20-daily.md`는 **손으로 만든 초기 샘플**이라
지금 파이프라인이 뽑는 것보다 **눈에 띄게 품질이 낮다.** 실측 비교:

| | 07-20 샘플 | 프로덕션(07-21~28) |
|---|---|---|
| ★1 점수 | **0개** | 매일 5~6개 |
| ★4 비중 | **46%** | 26~30% |
| weekly 생성 | **전량 누락** | 07-27 152버킷 정상 |
| 리포트 표 | **7열 2단 배치**(헤더 빈 칸 발생) | 계약대로 3열 |
| 베스트/주의 | 베스트 2개·주의 없는 섹션 다수 | **5개 섹션 전부 1+1** |

**이 샘플만 보고 품질을 판단하면 실제보다 훨씬 나쁘게 나온다.** (실제로 이번 점검에서
샘플만 본 평가가 "weekly 6/6 위반, ★1 0개, 표 계약 5/6 위반"이라는 잘못된 결론을 냈다.)
사이트 첫 화면에도 노출되는 데이터이므로 **재생성하거나 삭제하는 걸 권한다** —
`FORTUNE_DATE=2026-07-20`으로 워크플로를 수동 실행하면 지금 품질로 덮어쓸 수 있다.

---

## 5. 설계 원칙 위반 — 합/충을 AI가 계산하고 있다

CLAUDE.md와 기획서가 공통으로 못 박은 규칙: **"사실 데이터는 스크립트로, AI는 판단·생성만."**
그런데 [role-tti.md](prompts/role-tti.md)는 이렇게 지시한다:

```
오늘 일진의 지지와 충(冲)하는 띠는 변수가 많은 날, 합(合)하는 띠는 순조로운 날
```

**합·충 관계표를 코드가 주지 않는다.** `manse.js`는 십성(十星)까지만 계산하고 합·충은 없다.
즉 AI가 매일 12지지의 충·삼합·육합을 **기억에 의존해 추론**하고 있다.

실제 산출물을 검증해보니 지금까지는 **전부 맞았다** — 오미합(午未合), 축미충(丑未沖), 묘유충(卯酉沖)
모두 정확하다. 리포트 전체에서 30회 이상 언급되는데 오류를 못 찾았다.

**하지만 보증이 없다.** 십성은 코드가 계산해서 검증 가능한데, 합·충은 틀려도 아무도 모른다.
`manse.js`에 `chungMap()` / `hapMap()`을 추가해 `context.json`에 실어 보내면 이 구멍이 막힌다.
(30줄 남짓이면 된다 — 십성 계산이 이미 같은 자리에 있다.)

---

## 6. 그 밖에 발견한 것

### 견고성 / 보안

- **역할 AI가 저장소 전체에 쓰기 권한을 갖고 있다.** `--permission-mode bypassPermissions`로 실행되고,
  "git 금지 · 다른 파일 수정 금지"는 **프롬프트 문장으로만** 막혀 있다. 기술적 강제가 없다.
  `--allowedTools "Read,Write"` 로 도구를 줄이면 실수의 여지가 사라진다.
- 실제로 07-28 리포트에 **"어제의 큰 물줄기와 달리"** 라는 문장이 있다. writer는 어제 데이터를 입력으로
  받지 않는데도 그렇게 썼다 — 저장소의 어제 파일을 읽었을 가능성이 높다. 결과는 정확했지만
  (07-27은 임인일, 물 기운이 맞다) **계약 밖의 동작**이다.
- `--max-turns` 제한이 없다. 세션이 헤매면 `timeout-minutes: 90`을 다 태운다.
  (07-21 사고 때 실제로 약 73분을 쓰고 아무것도 못 만들었다.)

### ⚠️ 페이지를 늘리기 전에 반드시 알아야 할 지뢰

8번 항목의 2·5번(주간운세 화면, 과거 운세 열람)을 만들 때 **바로 밟게 되는 함정**이다.

[app.js:42](docs/assets/js/app.js#L42)의 데이터 로드는 **문서 상대경로**를 쓴다:

```js
fetchJSON('data/daily/' + today + '.json')     // 선행 슬래시 없음
```

지금은 이 스크립트가 **홈(`/fortune_telling/`)에서만** 로드되기 때문에 무사하다.
하지만 리포트 상세 페이지(`/reports/2026/07/28/daily/`)처럼 **깊은 경로에서 app.js를 로드하는 순간**
`/reports/2026/07/28/daily/data/daily/....json` 을 찾다가 즉시 404가 난다.

새 페이지를 만들 계획이라면 **미리** 루트 기준 경로로 바꿔두는 게 안전하다:

```js
// index.html 등에서 주입
window.DATA_BASE = "{{ '/data/' | relative_url }}";
```

> 참고: `_config.yml`에 `baseurl`이 없어도 사이트가 멀쩡한 이유는, GitHub Pages 기본 플러그인
> `jekyll-github-metadata`가 프로젝트 사이트에 `baseurl: /fortune_telling`을 **자동 주입**하기 때문이다.
> 공식 문서는 그래도 `_config.yml`에 명시하기를 권한다 — 플러그인·환경변수 의존이 사라져 결정적이 된다.
> (명시하면 로컬 `jekyll serve` 때 `--baseurl ""` 가 필요해진다.)

### 사용자 경험

- **과거 운세를 다시 볼 수 없다.** `docs/data/daily/`에 9일치가 쌓여 있지만 index는 오늘 것만 읽는다.
  날짜를 고르는 UI가 없다. (리포트 아카이브는 산문만 제공)
- **타로 "다시 뽑기"가 무제한이다.** 마음에 안 들면 계속 뽑을 수 있어서 "하루 한 장"의 긴장감이 없다.
  기획서 §3의 "선택 = 사용자의 랜덤" 취지와 어긋난다.
- 프로필에 **초기화(삭제) 버튼이 없다.** 수정은 되지만 지우려면 개발자도구를 열어야 한다.
- `style.css`의 `.tarot-result` 클래스는 app.js가 쓰지 않는 죽은 코드다. (사소)

### 도메인 정확도 — 확인이 필요한 판단

`manse.js`의 별자리 경계가 통용 기준과 다르다:

| | 이 프로젝트 | 흔히 쓰는 기준 |
|---|---|---|
| 사수자리 | 11/23 ~ **12/24** | 11/23 ~ 12/21 |
| 염소자리 | **12/25** ~ 1/19 | 12/22 ~ 1/19 |
| 처녀/천칭 경계 | 9/23 / **9/24** | 9/22 / 9/23 |

주석은 "±1일 차이 있을 수 있음"이라 적어뒀지만 **최대 3일 차이**다.
12/22~12/24 생일인 사람은 염소자리인데 사수자리 운세를 받는다.
한국 잡지 관례 중에 12/25 기준을 쓰는 표가 실제로 있으므로 **틀렸다고 단정할 순 없고, 의도한 것인지
결정하고 주석에 근거를 남겨야 할 사안**이다.

### 계산 로직 검증 결과 (이상 없음)

직접 재계산해 확인했다:
- **60갑자 일진** — 기준점 2000-01-07 갑자일, 1900-01-01 갑술일 교차검증 통과. 07-28 = 계묘일 정확.
- **십성 10종** — 생(生)·극(剋) 방향과 음양 조합 전부 정확. context.json 값과 손계산 일치.
- **입춘 기준 연주** — 2026-02-03까지 을사(뱀), 02-04부터 병오(말)로 정확히 전환.

이 부분은 믿고 써도 된다.

### 프로젝트 문서 상태

- `.claude/CLAUDE.md`가 **템플릿 미기입 상태**다. "*채우기*" 표시가 10곳 남아 있다.
- CLAUDE.md는 설계 문서를 `docs/설계/`에 두라고 하는데, **이 프로젝트의 `docs/`는 Jekyll 사이트 루트다.**
  거기에 설계 문서를 넣으면 **사이트에 그대로 공개된다.** 경로 규칙 자체를 바꿔야 한다
  (예: `설계/` 또는 `_docs/`).
- 루트의 `2026-07-21 이후 할 것.md`가 커밋되지 않은 채로 있다. (내용: "이제 사이트 보면서 세부사항 정하기!!")

---

## 7. 잘 되고 있는 것 — 건드리지 말 것

인상 관리 차원이 아니라, 실제로 검증했더니 좋았던 부분이다.

- **집필 AI(writer)의 품질이 높다.** 매일 그날 일진의 오행을 근거로 총평을 새로 쓰는데, 9일치가
  전부 다른 글이다. 계묘일은 "이슬 같은 물이 새 가지를 적시는 날", 경자일은 "금생수로 기운이
  아래로 뻗는 날" 처럼 **근거가 데이터에 붙어 있다.** 이 부분은 기획 의도대로 작동한다.
- **리포트가 데이터를 정확히 옮긴다.** 07-26·27·28 세 편의 띠 운세 표(36항목)를 원본 JSON과
  기계적으로 대조했더니 **점수·키워드 불일치 0건**이었다. writer가 숫자를 지어내지 않는다는 뜻이다.
  ("데이터에 있는 내용만 종합하라"는 [writer.md](prompts/writer.md)의 지시가 지켜지고 있다.)
- **리포트가 출력 계약을 지킨다.** 프로덕션 3일치를 검사했더니 표는 계약대로 **3열**,
  베스트·주의는 **5개 섹션 전부 정확히 1개씩**이었다. (07-20 샘플은 7열·베스트 2개로 어겼는데,
  그건 손으로 만든 것이라 그렇다 — 4-6 참고.) 명리 적용도 정확하다: 07-28 계묘일에 닭띠를
  **묘유충(卯酉沖)**으로 ★1을 준 판단이 맞다.
- **부분 실패 격리가 실전에서 작동한다.** assemble.js가 타입별로 검증하고 불량만 빼는 구조라,
  07-20 로컬 샘플에서 weekly가 통째로 비었을 때도 daily는 정상 발행됐다.
- **금지사항 위반이 0건이다.** 684버킷 전수를 질병·죽음 단정, 투자/종목, 법률 판단, 공포 조장
  패턴으로 훑었다. 걸린 11건은 전부 오탐이었다 — "암시해요"의 `암`, 타로 13번 **카드 이름**인 `죽음`,
  구어체 `대박`. 특히 죽음 카드는 9일 내내 *"무서운 카드가 아니라 이제 끝내도 된다는 허락"* 처럼
  일관되게 건설적으로 풀어낸다. [role-tarot.md](prompts/role-tarot.md)의 "겁주지 마라" 지시가
  실제로 지켜지고 있다.
- **XSS 방어가 일관되다.** app.js가 AI 생성 텍스트를 전부 `textContent`로만 렌더링한다.
  `innerHTML` 사용처가 한 군데도 없다.
- **커밋·푸시 순서 수정(`9eb8037`)이 정확하다.** rebase에서 `-X theirs`가 "이번 실행 산출물 우선"이
  맞다(rebase는 양쪽이 뒤집힌다). 주석의 설명도 정확하다. 이후 8일간 충돌 사고가 없었다.
- **만세력 단일 원본 관리.** `scripts/lib/manse.js` 하나를 assemble이 브라우저용으로 복사해서
  서버/클라이언트 계산이 갈라질 수 없는 구조다.

---

## 8. 손대는 순서 (제안)

효과 대비 비용 순으로 정렬했다. 위에서 셋만 해도 체감이 크게 달라진다.

### 아직 남은 것

| 순위 | 할 일 | 어디를 | 왜 |
|---|---|---|---|
| 1 | **어제 기억 주입** — 최근 3일 keyword를 프롬프트에 넣고 "겹치지 마라" 지시 | `context.js` + `prompts/common.md` | 4-1·4-2·4-3을 한 번에 해결. **재방문 이유가 생긴다** |
| 2 | **주간/월간/신년 운세 화면** — 주기 선택 UI | `docs/` 페이지 + `app.js` | 만들어 둔 데이터가 여전히 사장돼 있다 (3-1) |
| 3 | **사주 오프닝 고정 해제** — 별칭을 첫머리에 두지 말 것, 십성 이름 직접 노출 금지 | `prompts/role-saju.md` | 매일 10/10이 같은 골격 + 표와 중복 |
| 4 | **합·충 계산을 코드로** — `chungMap()`/`hapMap()` 추가 | `scripts/lib/manse.js` + `context.js` | 설계 원칙 위반 해소, 환각 여지 제거 (5절) |
| 5 | **07-20 샘플 재생성** — `FORTUNE_DATE=2026-07-20` 수동 실행 | `work/`·`docs/` | 실제보다 나쁜 품질이 박제돼 있다 (4-6) |
| 6 | **과거 운세 열람** — 날짜 선택 UI | `docs/` 페이지 + `app.js` | 9일치 데이터가 이미 있다 |
| 7 | **문체 다양화** — advice 어미 규칙("~세요" 70% 이하) | `prompts/common.md` | 현재 **92%**가 "~세요" 종결 |
| 8 | **본문 면책 한 줄** — 리포트 끝에 "재미로 보는 참고용" | `prompts/writer.md` | 푸터엔 있지만 본문만 퍼가면 사라진다 |
| 9 | **문서 정합성** — CLAUDE.md 채우기(*채우기* 10곳), `docs/설계` 경로 문제 | 문서 전반 | `docs/`는 사이트 루트라 설계문서를 두면 공개된다 |
| 10 | **별자리 경계 결정** — 12/22 기준으로 바꿀지 유지할지 | `scripts/lib/manse.js` | 판단 후 근거 주석 |

### 2026-07-28에 이미 처리한 것

| 할 일 | 결과 |
|---|---|
| 혈액형·MBTI 제거 | 프롬프트·스크립트·워크플로·사이트 전부 반영, 56버킷 |
| 운세별 독립 페이지 재편 | `saju`·`tti`·`zodiac`·`tarot` 4개 페이지 신설, 홈은 메뉴만 |
| 타로 3종 결함 수리 | `lucky.number` 분리, keyword 복창 금지, 최소 8장 일진 결부 |
| 타로 점수 규칙 교체 | 카드 고정 이미지 → 3점 출발 후 일진·요일 궁합으로 ±2 |
| 타로 하루 1회 | 날짜 키로 고정, 다시 뽑기는 확인 후에만 |
| 상대경로 지뢰 제거 | `window.DATA_BASE`를 Jekyll이 주입 |
| 프로필 지우기 버튼 | 사주 페이지에 추가 |
| 문서 스키마 드리프트 | 기획서 §5 순서·§7 `types` 래퍼 정정 |

### 지금 바로 할 것

```
git pull --rebase        # 로컬을 원격과 맞춘다 (8커밋 뒤처짐)
```

---

## 부록 — 확인에 쓴 방법

- 원격 산출물은 작업본을 건드리지 않으려고 `git archive origin/main`으로 별도 추출해 검사했다.
- 점수·키워드·문체 통계는 9일치 684버킷을 전수 집계한 값이다(표본 아님).
- 라이브 사이트는 실제 HTTP 요청으로 확인했다. `baseurl` 미설정이 걱정거리였으나,
  GitHub Pages가 프로젝트 사이트에 자동 주입해서 `/fortune_telling/assets/...`로 정상 해석된다.
  **문제 없음** (실측 확인).
- 만세력·십성은 코드를 신뢰하지 않고 독립적으로 재계산해 대조했다.
- **컨텐츠 품질 판정은 07-20 샘플을 제외하고 프로덕션 8일(07-21~28)만 대상으로 했다.**
  이번 점검 중 샘플만 본 별도 평가가 "weekly 6/6 위반 · ★1 0개 · 표 계약 5/6 위반"이라는
  결론을 냈는데, 프로덕션 데이터로 검증하니 **셋 다 사실이 아니었다.** 반대로 타로
  `lucky.number` 95% 일치, keyword 복창 53%, 사주 오프닝 10/10 고정, advice 92% 단일 어미는
  프로덕션에서도 그대로 재현돼 4-4·4-5에 반영했다. 이 저장소를 볼 때는
  **어떤 날짜의 산출물을 보고 있는지부터 확인해야 한다.**
